import { FilferCompletedPipe } from './filfer-completed.pipe';

describe('FilferCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilferCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
